모달모달리스,트레이알림영역만 시험


trymsg 코드 

--------------------------------

추가 -> 양식(form2)

ShowInTaskbar-> 상태표시줄
패널 이 뭔지 -> ?
패널이 왜 필요한지
FORM2창이있ㅎ는데 왜 패널을 위치시키는지

-> 
영화볼떄 스크린에 영사기 비춰짐
캔버스를 위치시키고 그 위에 사진이든 영상이든 위치시킴
FORM2라고 하는 창 위에 LABEL을 위치시킬거

링크라벨 -> 하이퍼 라벨을 통해서 텍스트에 입력한 거를 FORM2에 결과라고 하는 레이블에 표시할거다

텍스트에 글 입력하고 버튼눌렀더니 링크라벨글시가 띄워진다

버튼 3가지 기능(최소 3가지 버튼이 있다._
1. 서치를 위한 버튼(버튼을 눌르니 서치가됨)
2. 입력한 데이터를 전송하기위한(submit) 확인등과 관련된 버튼(추가 필요함 앞에 못들음)
3. RESET에 해당하는 버튼

보이기 -> submit(텍스트를 form2로 보내준다)

버튼을 클릭하면 어떤 이벤트를 발생시키겠습니까
btnMsg_Click

객체를생성해야한다 -> 이유 알아보기

show(),showDialog 차이점

shoDialog -> 모달로 열어서 처리한다

form1에서 form2로 메시지보내고 모달을 열어준다?

MsgText는 뭐지 -> 텍스트 전달을 눌렀어 그럼 텍스트가 전달이 될텐데 
어떻게 전달해 -> 값을 버튼을 눌러서 보내주는데 이런방식을 통해서 보낼거야
지난 주에 설명 -> 모달과 모달리스에서 form2,form3에 메시지 전달할수있는 보낼때 어떻게보냈을까

form2에 있는 객체있는 프로퍼티속성, 전달할때 값을 함부로 보내줘? 얘는 속성이다 속성을 가지고 전달한다. 값을 바꿀수가없다. 클래스의 멤버가 있는데 함부로 못바꿈 이 값은 속성으로 메시지를 전달

 
값을 가져올려면 정의를 해줘야함 TsgMsg

form1.cs -> 버튼을 누르니 form2 창이 열림 + 텍스트도 전달

텍스트를 어떻게 넘겨줄거냐 -> 속성으로 값이 넘겨줌

form2는 트레이창
트레이라는 것은 -> 모니터 안에 트레이 창이 위치 처음에는 안보이다가 일정한 시간이 지나면서 위로 이동 form2에 타이머 컨트롤 안 갔다둠 그럼 뭔가 이해가 안된다
뭐가? -> 트레이창이 숨겨져있다 서서히 올라온다 (투명도 ope~~( 속성) 기억안남
근데 타이머가 없다 어떻게 처리할건가 -> 

c#에서 제공하는 타이머 3가지
1. 윈도우에서 제공 도구(컨트롤에 타이머) 타이머 -> 특별히 조작할 필요가 없다 이 자체로 이벤트 발생시킬수있다.
2. 스레딩 타이머 (스레드를 동작시킬때 처리하는 타이머)
3. 타이머즈 클래스 -> 클래스를 통해 객체를 생성하고 객체생성한 타이머로 이벤트

타이머가 없으니 코드로 구현 3번

타이머 이벤트 발생시키기

타이머 클래스를 위치시킨다.-> using System.Timers; 선언해준다

일반적인 스크린 에 primaryscreen-> 이 있을거고 이 안에 작업영역이 있을거고
작업영역 -> 작업할수있는 공간영역의 넓이 높이, 
좌표값 찍은거다 width, height -> 여기에 트레이가 위치할수있는 좌표값이 생성

트레이 사이즈를 잡아둬야함 

패널을 ㅗ깍 안채워서 form2가 선택이 됨 form2 선택한채로 더블클릭

델리게이트 선언 이유 -> form2안에 메인있고 form1에 메인있다. 
현재 실행중인 게 프로세스 

충돌회피를 위해 델리게이트선언 지가 직접 관여하는게 아니라 form2에 메인에 관장한느 인보커 메서드를 통해 처리해달라고 부탁  인보커 함수나옴

















이름을 바꿔야한다 이름 안바꾸면오류 속성:name

dock -> none으로 놔두셈 패널이 form2를 완전이 뒤덮어서 로드시키는 코드를 작성못함
나중에바꾸셈 코드작성하고
